import { PropertySchema } from "./property-schema";

/**
 * Primary key schema definition.
 */
export class PkSchema {
  /** Properties that defines primary key */
  properties: PropertySchema[] = [];
}